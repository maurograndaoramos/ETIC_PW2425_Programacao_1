funcao calculaPrecoFinal(preco):
    se preco > 100: # desconto de 10%
        retorna preco * 0.9
    retorna preco


entrada("Diga um preco: ", x)

imprime(
    "o preco final é de: {calculaPrecoFinal(x)}"
)